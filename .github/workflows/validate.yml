name: Validate

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python tools/validate_package.py .
      - run: python -m py_compile skills/agent-skill-catalog/scripts/build_catalog.py skills/agent-skill-catalog/scripts/serve_catalog.py skills/agent-skill-catalog/scripts/import_legacy_catalog.py tools/validate_package.py tools/package_skill.py
      - run: python tests/test_build_catalog.py
      - run: python tools/package_skill.py .
      - uses: actions/upload-artifact@v4
        with:
          name: agent-skill-catalog
          path: dist/agent-skill-catalog-skill.zip*
