interface:
  display_name: "Agent Skill Catalog"
  short_description: "Build a searchable local skills catalog"
  default_prompt: "Use $agent-skill-catalog to catalog explicit local Skill and plugin roots, collect observed GitHub previews, and complete the Chinese description enrichment queue before reporting success."
policy:
  allow_implicit_invocation: false
